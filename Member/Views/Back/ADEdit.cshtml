@{
    ViewBag.Title = "AD";
}

<h2 class="mt-3">修改廣告</h2>

@using (Html.BeginForm("AD", "Back", FormMethod.Post, new { enctype = "multipart/form-data" }))
{
    <p>
        <input id="ADimg" name="ADimg" type="file" class="mt-3 mb-3"/>
    </p>
    <p>
        <input id="Submit" type="submit" value="送出" class="btn btn-outline-secondary"/>
    </p>

}